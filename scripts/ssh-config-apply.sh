# Step 1 — Save Config on your local machine:

nano ~/.ssh/config

# Paste the config → Save

# Step 2 — Set Permissions

chmod 600 ~/.ssh/config

# Step 3 — Connect to Bastion

# ssh bastion

# Step 4 — Connect to Private App

# ssh private-app
